// app/db.server.ts — placeholder.
// Singleton do Prisma Client (server-side).
//
// Quando implementado (Fase 8):
// - import { PrismaClient } from '@prisma/client';
// - export const prisma = globalThis.prisma ?? new PrismaClient();
// - if (NODE_ENV !== 'production') globalThis.prisma = prisma;
//
// Schema em prisma/schema.prisma. Mínimo inicial: Session (auth).
// Modelos de domínio (Tenant, AgentRun, etc.) virão depois.

export {};
