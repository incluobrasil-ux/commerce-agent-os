// app/shopify.server.ts — placeholder.
// Init do Shopify App SDK (server-side).
//
// Quando implementado (Fase 8):
// - shopifyApp({
//     apiKey, apiSecretKey, scopes (de SHOPIFY_SCOPES),
//     appUrl, sessionStorage (Prisma adapter via db.server),
//     hooks: { afterAuth: ... },
//     webhooks: { topic → handler },
//   })
// - Exports: authenticate, addDocumentResponseHeaders, sessionStorage, etc.
//
// Referência: 01_upstreams/shopify-app-template-react-router/app/shopify.server.ts
// Nomes canônicos de env vars: @cao/shared-config SECRET_NAMES.

export {};
