{
  "name": "@cao/creative-ops-service",
  "version": "0.0.0",
  "private": true,
  "description": "Fábrica de criativos — copy + imagens + vídeos via provedores externos",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "echo \"placeholder: tsx watch src/server.ts\"",
    "start": "echo \"placeholder: node dist/server.js\"",
    "typecheck": "tsc --noEmit",
    "test": "echo \"placeholder: test\"",
    "clean": "rm -rf dist .tsbuildinfo"
  }
}
