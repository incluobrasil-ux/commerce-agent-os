// app/root.tsx — placeholder.
// Layout raiz do app embedded.
//
// Quando implementado (Fase 8), exporta:
// - loader: autentica via shopify.authenticate.admin(request)
// - default component:
//     <AppProvider>          {/* Polaris */}
//       <NavMenu>...</NavMenu>{/* App Bridge */}
//       <Outlet />            {/* React Router */}
//     </AppProvider>
// - links: Polaris CSS + App Bridge script
//
// Referência (não copiar): 01_upstreams/shopify-app-template-react-router/app/root.tsx
//
// Origem: a popular ao clonar template oficial.

export {};
