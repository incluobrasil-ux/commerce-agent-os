// src/server.ts — placeholder.
//
// Quando implementado (Fase 9):
// - Bootstrap mínimo (sem HTTP público; apenas health endpoint para liveness)
// - Init de @cao/observability
// - Init de worker pool consumindo fila do merchant-service
// - Registro de pipelines (ingest → optimize → propose → return)

export {};
