// @cao/shared-schemas — placeholder
// Schemas runtime (zod por padrão; confirmar em ADR futuro).
// Espelham os tipos de @cao/shared-types e validam dados no boundary
// (HTTP, fila, DB, LLM, FS) antes do uso interno.
//
// Schemas planejados:
// - TenantIdSchema, AgentNameSchema
// - ProductEventSchema, CreativeBriefSchema, FeedRowSchema
// - AgentAuditEntrySchema, ResultEnvelopeSchema
// - LocaleSchema, CurrencySchema, MoneySchema

export {};
