// commitlint config raiz (ADR-0017).
// Conventional Commits 1.0.0 via @commitlint/config-conventional.
// Arquivo é ESM (root package.json tem "type": "module").

export default {
  extends: ['@commitlint/config-conventional'],
};
