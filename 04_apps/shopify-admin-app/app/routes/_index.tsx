// app/routes/_index.tsx — placeholder.
// Rota raiz quando não autenticado (página de instalação).
// Quando autenticado, App Bridge redireciona para /app.
//
// Estrutura mínima esperada (Fase 8):
// - loader: redirect para /app se sessão existir; senão retorna {showForm: true}
// - default: <Form action="/auth/login"> com input "shop" (your-store.myshopify.com)

export {};
