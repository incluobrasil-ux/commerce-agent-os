// vite.config.ts — placeholder.
// Config real (React Router + Shopify plugin) será adicionada na Fase 8,
// quando o template oficial estiver clonado em 01_upstreams/.
//
// Mínimo esperado:
// - @react-router/dev/vite plugin
// - @shopify/shopify-app-react-router preset
// - server: { host, port, hmr } compatível com túnel cloudflared

export default {};
